import { Document } from 'mongoose';

// export const getRefId = <T extends Document, FN extends keyof T>(obj: T, name: FN): string => {
//   return obj.populated(name.toString()) ? obj[name].id : obj[name];
// };
